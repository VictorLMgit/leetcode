/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var searchRange = function(nums, target) {
    


};

/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var binarySearch = function(nums, target, init, end) {
    const mid = (init + end) / 2;
    if (nums[mid] == target) return mid;
    if (nums[mid] > target)  return;
}

